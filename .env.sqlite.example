# SQLite fallback-only configuration for smoke mode and source export helpers.
# If primary DATABASE_URL points to Postgres, export helpers will prefer SQLITE_DATABASE_URL.
SQLITE_DATABASE_URL="file:./local.db"
DATABASE_URL="file:./local.db"
DB_QUERY_LOG="false"